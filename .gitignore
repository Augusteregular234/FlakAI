# Python
backend/venv/
**/__pycache__/
**/*.pyc
**/*.pyo
backend/flak_ai.db
backend/uploads/
backend/clips/

# Node
node_modules/
frontend/node_modules/
frontend/.next/
frontend/out/

# Dataset generado (opcional: quitar esta línea si quieres versionar manifests pequeños)
datasets/manifest.jsonl

# Env
.env.local
.env

# ML checkpoints (large files — track .pt manually when needed)
models/*.pt
models/*.onnx
models/training_metrics.json
models/training_history.json
models/incremental_metrics.json
models/training_status.json
models/active_model.txt
models/onnx_export_meta.json
models/dataset_state.json
checkpoints/*.pt

# Torch model cache
*.pth

# DirectML training venv (Python 3.12, local only)
training/training_venv/

# Runtime logs
overnight_log.txt
backend/regen_log.txt

# OS
.DS_Store
Thumbs.db

# Bases de datos (datos reales — nunca publicar)
*.db
flak_ai.db
training/flak_ai.db

# Config / herramientas locales
.claude/
.stfolder/
.ruff_cache/
CLAUDE.md

# Material de lanzamiento (estrategia interna — no publicar)
launch-kit/
README.original.md

# Logs de entrenamiento / runtime
*.log
models/training_error.log
models/training_output.log
